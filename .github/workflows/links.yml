name: Links

# External URLs in the documentation, checked on a schedule rather than on every pull request:
# third-party sites go down on their own timetable, and a broken page elsewhere must never block
# an unrelated merge. Internal links and anchors are gated per pull request in ci_pr.yml, offline.

permissions:
    contents: read

on:
    schedule:
        # Mondays 06:17 UTC - off the hour to dodge the top-of-hour rush.
        - cron: "17 6 * * 1"

    workflow_dispatch:

jobs:
    external-links:
        name: Check external links
        runs-on: ubuntu-latest
        timeout-minutes: 10

        steps:
            - name: Checkout repository
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            # lychee is installed from its pinned release archive with a checksum check rather
            # than through lycheeverse/lychee-action: the organisation runs GitHub-owned actions
            # only, and a verified download keeps that policy - and the trust it means - intact.
            - name: Install lychee
              shell: bash
              env:
                LYCHEE_VERSION: v0.24.2
                LYCHEE_SHA256: 1f4e0ef7f6554a6ed33dd7ac144fb2e1bbed98598e7af973042fc5cd43951c9a
              run: |
                curl -sSfL -o lychee.tar.gz "https://github.com/lycheeverse/lychee/releases/download/lychee-${LYCHEE_VERSION}/lychee-x86_64-unknown-linux-gnu.tar.gz"
                echo "${LYCHEE_SHA256}  lychee.tar.gz" | sha256sum --check --strict
                tar -xzf lychee.tar.gz
                sudo install -m 755 lychee-x86_64-unknown-linux-gnu/lychee /usr/local/bin/lychee
                rm -rf lychee.tar.gz lychee-x86_64-unknown-linux-gnu
                lychee --version

            # Transient failures are retried, and the rate-limit status GitHub hands anonymous
            # crawlers is accepted, so a red run means a link is dead rather than merely busy.
            - name: Check every link, external included
              run: lychee --include-fragments --no-progress --max-retries 3 --accept "200..=299,403,429" --root-dir "$(pwd)" --exclude-path build --exclude-path external --exclude-path node_modules --exclude-path tools/.venv "./**/*.md"
